# Docker Enterprise edition setup

To setup Anvitra using docker, run the following commands

```bash
cp sample.env .env
# example repo name
./update-repo-name "ecr-repo-name"
docker compose up -d
```
